# RTX 4070 Laptop (8 GB VRAM): responsive IDE and worker node.
# Load with: ./scripts/docker/up-node.sh rtx4070-laptop

LLAMAFARM_NODE_NAME=llamafarm-rtx4070-laptop
LLAMAFARM_DEFAULT_ROLE=worker
LLAMAFARM_DISCOVERY_MODE=manual
LLAMAFARM_ALLOW_REMOTE_SUBAGENTS=true

# Publish only the direct-access LlamaFarm gateway to the LAN. Raw Ollama and Qdrant
# ports remain local to this host.
LLAMAFARM_HOST_BIND_IP=0.0.0.0
OLLAMA_HOST_BIND_IP=127.0.0.1
QDRANT_HOST_BIND_IP=127.0.0.1
LLAMAFARM_GATEWAY_HOST=0.0.0.0
LLAMAFARM_ALLOW_PUBLIC_BIND=true

# q8_0 KV cache and Flash Attention make this a quality-preserving context
# profile rather than an overcommitted one. The interactive model comes from
# persisted LlamaFarm config unless an operator explicitly sets LLAMAFARM_MODEL.
OLLAMA_CONTEXT_LENGTH=32768
OLLAMA_NUM_CTX=32768
OLLAMA_NUM_PARALLEL=1
# Keep BOTH the chat model and the small embed model (nomic-embed-text, ~0.3GB)
# resident. With 1 slot, every memory/RAG recall loaded the embed model and
# evicted the chat model, forcing a ~40s chat-model reload on the next turn
# (the dominant TTFT cost). qwen3.5:9b (6.2GB) + embed (0.3GB) + overhead fit
# in 8GB, so 2 slots removes the thrash.
OLLAMA_MAX_LOADED_MODELS=2
OLLAMA_KV_CACHE_TYPE=q8_0
OLLAMA_FLASH_ATTENTION=1
OLLAMA_GPU_OVERHEAD=805306368
OLLAMA_KEEP_ALIVE=10m

# Compact context proactively and retain the recent task/tool turns. Older
# turns are summarized into memory/WORKING_STATE.md and SQLite memory.
LLAMAFARM_AGENT_COMPACT_CONTEXT=true
LLAMAFARM_AGENT_TOOL_ROUTING_ENABLED=true
LLAMAFARM_AGENT_TOOL_ROUTING_TOP_K=12
LLAMAFARM_AGENT_MAX_HISTORY_MESSAGES=32
LLAMAFARM_AGENT_MAX_TOOL_ITERATIONS=64
LLAMAFARM_AGENT_PARALLEL_TOOLS=false
# This is a continuation/checkpoint boundary, not a job timeout. Long jobs
# automatically start another segment with their verified plan state intact.
LLAMAFARM_AGENT_MAX_OUTPUT_TOKENS=4096

# LlamaFarm and Qdrant intentionally have no Compose CPU, memory, swap, or PID
# ceilings. The local box is allowed to use all available host capacity.

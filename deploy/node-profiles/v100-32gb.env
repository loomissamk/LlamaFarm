# Tesla V100 PCIe (32 GB HBM2): Volta CUDA coordinator and quality worker.
# Load with: ./scripts/docker/up-node.sh v100-32gb

LLAMAFARM_NODE_NAME=llamafarm-v100-32gb
LLAMAFARM_DEFAULT_ROLE=both
LLAMAFARM_DISCOVERY_MODE=manual
LLAMAFARM_ALLOW_REMOTE_SUBAGENTS=true

# Sparse MoE quality default, sized to keep its weights resident on a 32 GB
# V100 while adaptive context requests grow only when needed.
LLAMAFARM_INITIAL_MODEL=qwen3.6:35b-a3b-mtp-q4_K_M

# This profile is CUDA-only and refuses broad GPU visibility. Put the exact
# V100 UUID in the private node.env, and keep it identical to V100_GPU_UUID in
# the root-owned host profile. The 5070 Ti Nouveau/NVK render nodes are not
# passed into this container.
LLAMAFARM_V100_HOST_CONFIG=${LLAMAFARM_V100_HOST_CONFIG:-/etc/llamafarm/v100-cuda-5070-nvk.env}
LLAMAFARM_NVIDIA_REQUIRE_EXACT_UUID=true
LLAMAFARM_NVIDIA_COMPUTE_ONLY=true
LLAMAFARM_EXPOSE_DRI=false

LLAMAFARM_HOST_BIND_IP=0.0.0.0
OLLAMA_HOST_BIND_IP=127.0.0.1
QDRANT_HOST_BIND_IP=127.0.0.1
LLAMAFARM_GATEWAY_HOST=0.0.0.0
LLAMAFARM_ALLOW_PUBLIC_BIND=true

# Direct Ollama calls use 64K. LlamaFarm dashboard Auto uses the selected
# model's native maximum; manual dashboard presets remain exact.
OLLAMA_CONTEXT_LENGTH=65536
OLLAMA_NUM_CTX=65536
LLAMAFARM_ADAPTIVE_CONTEXT=false
LLAMAFARM_ADAPTIVE_CONTEXT_MAX=262144
OLLAMA_KV_CACHE_TYPE=q8_0
OLLAMA_FLASH_ATTENTION=1
OLLAMA_KEEP_ALIVE=-1

# Preserve complete working history and remove profile-imposed task budgets.
LLAMAFARM_AGENT_COMPACT_CONTEXT=false
LLAMAFARM_AGENT_TOOL_ROUTING_ENABLED=true
LLAMAFARM_AGENT_TOOL_ROUTING_TOP_K=12
LLAMAFARM_AGENT_MAX_HISTORY_MESSAGES=0
LLAMAFARM_AGENT_MAX_TOOL_ITERATIONS=0
LLAMAFARM_AGENT_PARALLEL_TOOLS=false
# Reasoning/output and no-progress limits live in [agent] config. V100-hosted
# larger models can raise them independently from the dashboard.
LLAMAFARM_WS_PERSISTED_MAX_SESSIONS=0
LLAMAFARM_SCHEDULER_MAX_CONCURRENT=0
LLAMAFARM_SCHEDULER_MAX_TASKS=0

# Compose intentionally imposes no CPU, memory, swap, or PID ceilings.
